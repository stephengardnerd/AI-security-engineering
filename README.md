# AI engineering

Personal workspace for AI engineering coursework, learning notes, and experiments.

## Local configuration

Credentials are stored locally in `.env`. Environment files (`.env`, `.env.*`, and `*.env`) are excluded from version control. Never commit credentials or secrets.
